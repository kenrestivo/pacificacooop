#!/bin/sh

#$Id$
# nightly cron job to back up all of the key databases
# in the live version of the app

WHERE=/mnt/kens/ki/proj/coop/sql

cd $WHERE

#this sucks. i can't do it... PASSWORDS!!!
#make ssh-tunnel && make backup-live

#EOF
